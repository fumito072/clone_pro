# Local run (controller + STT + LLM)
-r ears_stt/requirements.txt
-r head_llm/requirements.txt

# Optional: VM-side TTS server (GPU VM)
# -r api_server/requirements.txt

# Optional: LoRA training utilities
# -r mouth_tts/requirements_lora.txt
